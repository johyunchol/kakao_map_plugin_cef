import 'package:kakao_map_plugin_example/main.dart' as app;

/// Windows / Linux 에서 예제 앱을 그대로 실행합니다.
///
/// `kakao_map_plugin_cef` 가 pubspec 에 있으므로 CEF 브릿지가 자동 등록됩니다.
/// 카카오 콘솔에 `http://localhost:8790` 이 등록되어 있어야 합니다.
void main() => app.main();
